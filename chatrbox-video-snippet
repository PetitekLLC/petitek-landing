<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Chatrbox Video Snippet</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background: #f9f9f9;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    button {
      font-size: 18px;
      padding: 14px 24px;
      border: none;
      border-radius: 6px;
      background-color: #25A8C6;
      color: white;
      cursor: pointer;
    }
    #videoModal {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      background: rgba(0,0,0,0.85);
      z-index: 9999;
      justify-content: center;
      align-items: center;
    }
    #videoModal iframe {
      width: 90%;
      height: 220px;
      border: none;
      border-radius: 8px;
    }
    #closeBtn {
      position: absolute;
      top: 10px;
      right: 10px;
      font-size: 20px;
      background: white;
      border: none;
      border-radius: 50%;
      width: 30px;
      height: 30px;
      cursor: pointer;
    }
  </style>
</head>
<body>

  <button onclick="playSegment()">▶ Click to Watch</button>

  <div id="videoModal">
    <div style="position: relative; width: 100%; max-width: 600px;">
      <iframe id="vimeoPlayer" src="" allow="autoplay; fullscreen" allowfullscreen></iframe>
      <button id="closeBtn" onclick="closeModal()">&times;</button>
    </div>
  </div>

  <script src="https://player.vimeo.com/api/player.js"></script>
  <script>
    const modal = document.getElementById("videoModal");
    const iframe = document.getElementById("vimeoPlayer");
    let player;

    function playSegment() {
      iframe.src = "https://player.vimeo.com/video/1073251548#t=2m49s";
      modal.style.display = "flex";

      player = new Vimeo.Player(iframe);

      player.setCurrentTime(169); // 2:49 in seconds
      player.play();

      const endTime = 194; // 3:14 in seconds

      const interval = setInterval(() => {
        player.getCurrentTime().then(seconds => {
          if (seconds >= endTime) {
            clearInterval(interval);
            closeModal();
          }
        });
      }, 500);
    }

    function closeModal() {
      modal.style.display = "none";
      player.unload();
    }
  </script>
</body>
</html>
